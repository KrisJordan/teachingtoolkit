[project]
name = "teachingtoolbox"
version = "0.1.0"
description = "Focused Zensical AI tool directory for UNC instructors"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "zensical>=0.0.45",
]
